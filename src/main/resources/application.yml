spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:
#  h2:
#    console:
#      enabled: true
#      path: /h2
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
  data:
    redis:
      host:
      password:
      port:

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
jwt:
  token:
    secretKey:
    accessTokenLifeTime: 30
    refreshTokenLifeTime: 60
authority:
  id:
    admin:
